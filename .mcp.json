{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@0.0.82",
        "--browser", "chrome",
        "--viewport-size", "1440,900"
      ]
    }
  }
}
